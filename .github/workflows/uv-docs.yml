name: Build Documentation (using UV)

on:
  workflow_call:
    inputs:
      version:
        description: 'Version number for the documentation'
        required: true
        type: string
      build-pdf:
        description: 'Whether to build the PDF version of the documentation (much faster if not)'
        required: false
        type: boolean
        default: true
      pdf-name:
        description: 'Name of the PDF file to generate'
        required: true
        type: string
      docs-folder:
        description: "The path to the docs folder from the root of the project, defaults to docs"
        default: docs
        required: false
        type: string
      makefile:
        description: "The makefile to use, defaults to Makefile"
        default: Makefile
        required: false
        type: string
      lfs:
        description: "Whether to pull LFS files, defaults to false"
        required: false
        type: boolean
        default: false
      submodules:
        description: "Whether to pull git submodules (false|true|recursive), defaults to false"
        required: false
        type: string
        default: 'false'
      dependencies:
        description: "Additional system (apt) dependencies"
        required: false
        type: string
      latex-packages:
        description: "Additional apt packages to install alongside the LaTeX toolchain, space separated (e.g. 'texlive-fonts-extra')"
        required: false
        type: string
        default: ''
      timeout-minutes:
        description: "Maximum duration of the documentation build job, in minutes"
        required: false
        type: number
        default: 30
      env:
        description: "Additional environment variables for the build steps, as newline-separated KEY=VALUE pairs"
        required: false
        type: string
        default: ''
      artifact-suffix:
        description: "Suffix (without leading '-') appended to the uploaded HTML artifact name and the PDF filename, to distinguish multiple builds of the same version (e.g. from a matrix). Only [A-Za-z0-9._-] is accepted; anything else fails the build"
        required: false
        type: string
        default: ''
    secrets:
      ssh-key:
        description: "SSH private key to use for checking out private submodules"
        required: false

jobs:
  build-docs:
    runs-on: ubuntu-24.04
    timeout-minutes: ${{ inputs.timeout-minutes }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          ssh-key: ${{ secrets.ssh-key }}
          lfs: ${{ inputs.lfs }}
          submodules: ${{ inputs.submodules }}

      - name: Install uv
        uses: astral-sh/setup-uv@v8.2.0
        with:
          version: "0.6.x"

      - name: Set up Python
        run: uv python install

      - name: Install system dependencies for LaTeX
        if: ${{ inputs.build-pdf }}
        timeout-minutes: 15
        env:
          # Passed through the environment rather than interpolated, so that a caller's
          # input cannot inject shell code.
          EXTRA_PACKAGES: ${{ inputs.latex-packages }}
        run: |
          set -euo pipefail
          # Word splitting of APT_OPTS and EXTRA_PACKAGES is intentional below.
          APT_OPTS="-o Acquire::Retries=3 -o Acquire::http::Timeout=30 -o Acquire::https::Timeout=30"
          for attempt in 1 2 3; do
            # shellcheck disable=SC2086
            sudo apt-get $APT_OPTS update && break
            if [ "$attempt" -eq 3 ]; then
              echo "apt-get update failed after 3 attempts" >&2
              exit 1
            fi
            echo "apt-get update failed (attempt $attempt), retrying" >&2
            sleep $((attempt * 15))
          done
          # shellcheck disable=SC2086
          sudo apt-get $APT_OPTS install -y --no-install-recommends \
            texlive-latex-recommended \
            texlive-latex-extra \
            texlive-fonts-recommended \
            tex-gyre \
            latexmk \
            $EXTRA_PACKAGES

      - name: Install additional system dependencies
        if: ${{ inputs.dependencies != '' }}
        timeout-minutes: 15
        env:
          # Passed through the environment rather than interpolated, so that a caller's
          # input cannot inject shell code.
          PACKAGES: ${{ inputs.dependencies }}
        run: |
          set -euo pipefail
          # Word splitting of APT_OPTS and PACKAGES is intentional below.
          APT_OPTS="-o Acquire::Retries=3 -o Acquire::http::Timeout=30 -o Acquire::https::Timeout=30"
          for attempt in 1 2 3; do
            # shellcheck disable=SC2086
            sudo apt-get $APT_OPTS update && break
            if [ "$attempt" -eq 3 ]; then
              echo "apt-get update failed after 3 attempts" >&2
              exit 1
            fi
            echo "apt-get update failed (attempt $attempt), retrying" >&2
            sleep $((attempt * 15))
          done
          # No --no-install-recommends here: callers pass arbitrary packages and may rely on recommends.
          # shellcheck disable=SC2086
          sudo apt-get $APT_OPTS install -y $PACKAGES

      - name: Set additional environment variables
        if: ${{ inputs.env != '' }}
        env:
          # Passed through the environment rather than interpolated, so that a caller's
          # input cannot inject shell code.
          EXTRA_ENV: ${{ inputs.env }}
        run: printf '%s\n' "$EXTRA_ENV" >> "$GITHUB_ENV"

      - name: Compute artifact suffix
        id: suffix
        env:
          ARTIFACT_SUFFIX: ${{ inputs.artifact-suffix }}
        run: |
          set -euo pipefail
          if [ -z "$ARTIFACT_SUFFIX" ]; then
            echo "value=" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # The suffix lands in artifact names and in the PDF filename, and is written to
          # `$GITHUB_OUTPUT` as a single unterminated line. Reject anything that would
          # break an upload or smuggle extra step outputs through an embedded newline.
          # A `case` glob is matched against the whole value; `grep` would test each line
          # separately and so accept a multi-line suffix.
          case "$ARTIFACT_SUFFIX" in
            *[!A-Za-z0-9._-]*)
              echo "::error::artifact-suffix must contain only [A-Za-z0-9._-]"
              exit 1
              ;;
          esac

          echo "value=-$ARTIFACT_SUFFIX" >> "$GITHUB_OUTPUT"

      - name: Create version file
        env:
          VERSION: ${{ inputs.version }}
        run: printf '%s\n' "$VERSION" > .version

      - name: Extract project name from conf.py
        id: project-name
        env:
          DOCS_FOLDER: ${{ inputs.docs-folder }}
        run: |
          set -euo pipefail

          # The script is written out with a quoted heredoc and reads the folder from the
          # environment, so neither the shell nor a caller-supplied path can reach the
          # Python source. It goes to a file rather than a heredoc inside `$( )`, which
          # bash mis-parses when the body contains quote characters.
          cat > "${RUNNER_TEMP}/project_name.py" <<'PY'
          import os
          import re

          try:
              with open(os.path.join(os.environ["DOCS_FOLDER"], "conf.py")) as f:
                  content = f.read()
          except OSError:
              print("no-name")
          else:
              match = re.search(r"""project\s*=\s*['"](.*?)['"]""", content)
              print(match.group(1).lower().replace(" ", "-") if match else "no-name")
          PY

          PROJECT_NAME=$(python "${RUNNER_TEMP}/project_name.py")
          echo "name=$PROJECT_NAME" >> "$GITHUB_OUTPUT"
          echo "Extracted project name: $PROJECT_NAME"

      - name: Build HTML documentation
        env:
          DOCS_FOLDER: ${{ inputs.docs-folder }}
          MAKEFILE: ${{ inputs.makefile }}
        run: make -f "$MAKEFILE" -C "$DOCS_FOLDER" html

      - name: Upload HTML documentation
        uses: actions/upload-artifact@v7
        with:
          name: documentation-${{ steps.project-name.outputs.name }}${{ steps.suffix.outputs.value }}-html-${{ inputs.version }}
          path: "${{ inputs.docs-folder }}/_build/html/"
          retention-days: 30

      - name: Build PDF documentation
        if: ${{ inputs.build-pdf }}
        env:
          DOCS_FOLDER: ${{ inputs.docs-folder }}
          MAKEFILE: ${{ inputs.makefile }}
        run: make -f "$MAKEFILE" -C "$DOCS_FOLDER" latexpdf

      - name: Rename PDF with version
        if: ${{ inputs.build-pdf }}
        id: rename-pdf
        env:
          DOCS_FOLDER: ${{ inputs.docs-folder }}
          PDF_NAME: ${{ inputs.pdf-name }}
          VERSION: ${{ inputs.version }}
          SUFFIX: ${{ steps.suffix.outputs.value }}
        run: |
          set -euo pipefail
          PDF_DIR="$DOCS_FOLDER/_build/latex"
          # Extract filename without extension
          PDF_BASE=$(basename "$PDF_NAME" .pdf)
          NEW_PDF="${PDF_BASE}${SUFFIX}-${VERSION}.pdf"
          mv "$PDF_DIR/$PDF_NAME" "$PDF_DIR/$NEW_PDF"
          echo "new-pdf-name=$NEW_PDF" >> "$GITHUB_OUTPUT"

      - name: Upload PDF documentation
        if: ${{ inputs.build-pdf }}
        uses: actions/upload-artifact@v7
        with:
          name: documentation-${{ steps.project-name.outputs.name }}${{ steps.suffix.outputs.value }}-pdf-${{ inputs.version }}
          path: "${{ inputs.docs-folder }}/_build/latex/${{ steps.rename-pdf.outputs.new-pdf-name }}"
